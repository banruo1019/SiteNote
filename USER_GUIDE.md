# SiteNote 使用说明

> 面向终端用户。两个角色:**工程师(Engineer)** 和 **项目经理(PM)**,根据你的实际工作选。

---

## 首次启动

1. **选角色** — 第一次打开 App 会让你选 "工程师" 或 "项目经理"。两种角色看到的界面完全不同。
2. **授权** — 第一次按住录音按钮会弹麦克风权限,允许。
3. **公司信息**(工程师才有)— 设置 → 我和公司 → 填公司名 / ABN / 上传 Logo。这些会出现在你导出的 PDF 报告封面。

---

# 第一部分:工程师(Engineer)

工程师版本 4 个 Tab:**记 / 报告 / 日程 / 设置**。

## 一、「记」Tab — 录入

### 录音
- 按住下方麦克风(132pt 黑色大圆按钮)→ 说话 → 松手保存
- 会自动识别工地(基于 GPS,需要你在设置里建了对应工地)
- 转写完成后会**立即跳到详情页**

### 拍照
- 按下方相机(132pt 白边圆按钮)→ 拍照
- 自动进入 **标注界面**:画笔 / 直线 / 文字 / 橡皮 / 颜色 / 撤销
- 保存后会进入 staged 区(可继续拍多张)
- 点 "完成" → 立即保存 + 跳详情页

⚠️ 工程师模式**没有 AI 分析照片**(关掉了),只是纯记录。

### 详情页
- 大字编辑转写文本
- 看照片 / 加照片 / 换照片
- 录音回放
- **平面图图钉**:点 "平面图" 折叠区 → 选已上传的图纸 → 点击位置打图钉
- 分享 / 删除

不会看到 PM 才有的:已处理 / 改期 / 标记隐患 / 转为施工日志 / AI 助手菜单。

## 二、「报告」Tab — 巡检报告

工程师工作流的终点:把多条记录合成一份 PDF 报告发给 builder。

### 新建一份报告
1. 顶部黑色大按钮 **+ 导出报告**
2. **Header 段**:工地预设(如果已建过预设,选了之后会自动 prefill Project No. / Client / Location / Attn / Inspection Type),不然手填
3. **选记录**:点 "选择记录(已选 N 条)"
   - 按工地 + 最近 N 天,或按时间段
   - 勾选要纳入报告的记录
   - 如果之前在别的筛选下选过的,在末尾会标灰"超出筛选"显示,可在此取消
4. **图片说明**:每条记录的描述默认是录音转写,可以改短或纠正
5. **导出 PDF** → 弹出预览 → 发送邮件 / 分享 PDF

### PDF 格式(自动)
- 第 1 页:封面(公司 Logo / Header 表 / Disclaimers / 签字栏)
- 每条记录单独一页(或多页):
  - 顶部 `#N` 蓝色大编号 + 时间 + 工地
  - 中间 **A3 横向图纸** 居中,红色十字标记位置
  - 下面描述文字
  - 底部照片网格(有图纸:1x2 大图;无图纸:2x2 网格)
  - 多于 2/4 张照片自动续页 `#N (cont.)`

### 发邮件 = 自动归档
- 选 builder 收件人(从联系簿)→ 主题 / 正文都可改 → 发送
- 发送成功后**报告自动归档**:报告 Tab 里从"草稿"段跳到"已提交"段

## 三、「日程」Tab — 排期

- 月历视图,点天看当天日程
- 新建日程:标题(如 "FFL steel" / "Roof slab") + 日期 + 时间 + 工地 + 备注
- **双提醒**:默认 1 天前 + 1 小时前,可改
- 状态:待执行 → 已完成 / 取消

## 四、「设置」Tab — 5 段精简

### 我和公司
- 我的角色(可切回 PM)
- 公司名 / ABN
- 公司 Logo(PDF 封面用)

### 工作资源
- **工地**:列表 + 新建(先输地址 → 自动派生工地名,可改)
- **工地预设**:每个工地的 Header 模板(Project No. / Client / Inspection Type 等),选 builder 联系人
- **平面图**:上传 + 管理(支持 PDF 或图片)
- **建造商联系簿**:Builder 名 / 邮箱 / 公司

### 报告
- 默认免责声明(PDF 封面用)

### 日程
- 默认提醒提前时间(1 天 / 1 小时 / 30 分钟 / 关闭)

### 数据和关于
- 导出全部数据 ZIP
- 垃圾桶(软删的 note,30 天后自动永久删)
- 语言(跟随系统 / 简体中文 / English)
- 反馈与建议
- 版本号

---

# 第二部分:项目经理(PM)

PM 版本 3 个 Tab:**今天 / 日志 / 报告**(以及设置 Tab)。

## 一、「今天」Tab — 主屏

### 顶部 stats 行(4 档)
- 逾期 / 今天 / 待分类 / 隐患
- 点击其中一项 = 高亮该类作为筛选;其他段保留显示(不会消失)

### 主体列表(5 段折叠)
- 隐患 → 逾期 → 今天到期 → 待分类 → 已完成
- 每段可独立展开 / 折叠
- 左滑:完成 / 取消完成;右滑:删除

### 底部
- 132pt 麦克风 + 相机大按钮(同 Engineer)
- 拍照后:**直接存** 或 **AI 分析**(PM 有 AI 助手)

## 二、「日志」Tab

两个模式切换:**纵览** / **台账**。

### 纵览
跨日总账,4 大段折叠:
- 要盯(隐患 + 逾期)
- 今天到期
- 待分类
- 归档(3 天 / 本周 / 以后 / 备忘 / 已完成)

### 台账
当日明细 + 工时簿:
- 顶部日期切换器 + 4 档摘要(到场人数 / 机械记录 / 未结束 / 速记)
- 4 sub-tab:人员 / 机械 / 事件 / 速记
- 底部 "生成今日 PDF" 按钮 → SiteDiary PDF

## 三、「报告」Tab

- 本周 sparkline(条形图)
- 本月汇总(人日 / 机械工时 / 隐患数)
- 本月细分(Top 5 工种 / Top 5 机械 / 隐患列表)
- 输出清单(PDF 巡检日志 / 周报 / EOT / AI 发现)

## 四、「设置」Tab

PM 版 6 段:
- 角色 / 语言
- 常用(提醒 / 报告与导出 / 数据与关于)
- 工地资源
- AI(录入与 AI 配置)

---

# 公共功能

## AI 模式说明
- **PM**:可全部启用 — 转写润色 / 自动分类 / 抽取工种机械 / 照片分析
- **Engineer**:**全部关闭**,转写是你的原话,没有 AI 加工

## 数据存储
- 全部本地存储(SwiftData + 文件系统)
- 录音 m4a / 照片 jpg 在 Caches 目录
- 暂不支持云同步(v1.2 计划)

## 数据迁移 / 换机
- 设置 → 数据 → 导出全部数据 ZIP
- 新机器导入(暂未实现,目前需要手动放回去)— v1.2 会做 iCloud 同步

## 常见问题

### Q. 录音不能用 / 没有声音
A. 设置 → 隐私 → 麦克风 → 找 SiteNote 打开

### Q. AI 不工作(PM)
A. 设置 → 录入与 AI → 高级配置 → 检查 OpenAI API Key 是否填了 + 是否过期

### Q. 工地没自动识别
A. 第一次去新工地需要手动选;之后 GPS 会自动学习(常去的位置 → 默认工地)。可在设置 → 工地预设 里也手动绑定地址

### Q. PDF 太大 / 邮件发不出去
A. 单份报告超过 25MB 时大部分邮箱会拒收。减少照片数量,或改用"分享 PDF"通过 AirDrop / WhatsApp / 文件 app

### Q. 报告写错了想改
A. 草稿状态可以一直改;已提交(发了邮件)的报告也能改,但建议另存为新草稿,保留历史
